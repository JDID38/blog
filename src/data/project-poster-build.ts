/**
 * Slugs that get a multipage HTML build under `projects/<slug>/`.
 * Keep this list in sync with `projectDetails` in `project-detail.ts` (same order as shown on the site).
 */
export const PROJECT_POSTER_BUILD_SLUGS = [
  "fluid-diffusion",
  "medical-image-synthesis",
  "marl-social-dilemmas",
  "soccer-financial-trends",
  "von-karman-cfd",
  "pump-body-design",
  "centrifugal-compressor",
  "motor-gearbox",
  "rerock-startup",
  "aircraft-brakes",
] as const
